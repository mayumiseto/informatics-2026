def celsius_to_fahrenheit(celsius):
    """摂氏温度を華氏温度に変換して返す。"""
    # TODO: 摂氏温度を華氏温度に変換し、return で返す。
    #
    # 復習の目安: 計算式・変数は第02回、関数とreturnは第08回です。
    # ヒント1: 使う式は「華氏温度 = 摂氏温度 * 9 / 5 + 32」です。
    # ヒント2: celsius という変数には、摂氏温度の値が入っています。
    # ヒント3: 計算結果を fahrenheit という変数に入れると分かりやすいです。
    # ヒント4: 最後は return fahrenheit のように、計算結果を返します。


def make_message(celsius, fahrenheit):
    """計算結果を小数点以下1桁で表示するメッセージを作成して返す。"""
    # TODO: 例: "25 deg C = 77.0 deg F" のような文字列を作り、return で返す。
    #
    # 復習の目安: 文字列と表示は第02回、関数とreturnは第08回です。
    # ヒント1: 文字列を作るには f文字列を使うと便利です。
    # ヒント2: 例: f"{celsius} deg C = {fahrenheit:.1f} deg F"
    # ヒント3: 「:.1f」は、小数点以下1桁で表示する指定です。
    # ヒント4: 最後は return message のように、作った文字列を返します。
